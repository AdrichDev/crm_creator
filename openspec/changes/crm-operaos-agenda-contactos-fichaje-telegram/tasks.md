# Tasks ? Agenda OperaOS, Contactos, Telegram UI y Fichaje

## Review Workload Forecast
| Field | Value |
|-------|-------|
| Estimated changed lines | 1400-2400 |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 agenda visual ? PR2 calendar/maps ? PR3 contactos ? PR4 Telegram UI ? PR5 fichaje |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

## Phase 1: Agenda visual
- [x] 1.1 Extraer gramática reusable desde `front/components/panel/widgets/agenda-widget.tsx`.
- [x] 1.2 Reemplazar `front/app/(crm)/citas/page.tsx` por vista full-screen equivalente.
- [x] 1.3 Tests de vista y terminología sectorial.

## Phase 2: Calendar CRUD y Google Maps
- [x] 2.1 Unificar create/update/delete contra Google Calendar en `back/src/lib/integrations/calendar.ts`.
- [x] 2.2 Asegurar idempotencia por `bookingId` en `back/src/routes/bookings.ts`.
- [x] 2.3 Sustituir mapa actual por URL/embed Google Maps y test de builder. (commit b795eba)

## Phase 3: Contactos
- [x] 3.1 Añadir `contactos` a módulos, tenant config y navegación. (commit 1ed67ca)
- [x] 3.2 Replicar UI/lógica desde `agents-agency/front/app/contactos` y `components/contactos`. (commit 1ed67ca)
- [x] 3.3 Crear/ajustar API y persistencia si el modelo no existe. (commit 1ed67ca)

## Phase 4: Telegram UI
- [x] 4.1 Modelar conversaciones/mensajes Telegram por tenant si falta persistencia.
- [x] 4.2 Crear vista UI de conversación en vivo.
- [x] 4.3 Añadir respuesta manual por Telegram con idempotencia.

## Phase 5: Fichaje
- [x] 5.1 Rediseñar `front/app/(crm)/fichaje/page.tsx` con selector intensiva/partida.
- [x] 5.2 Ajustar back/modelo para eventos ordenados de jornada si el CRUD actual no alcanza.
- [x] 5.3 Tests de secuencia válida y bloqueo de fichajes extra.

## Phase 6: Fixes reales tras QA (2026-07-05) — las fases 1-5 quedaron marcadas [x] pero no funcionaban en la práctica
- [x] 6.1 Padding lateral en todas las vistas del panel (`.opera-main`/`.opera-content`, `app-shell.tsx`) — antes el contenido ocupaba el 100% del ancho.
- [x] 6.2 Dashboard sobrecargado de widgets de agenda (4 de 5 widgets por defecto eran variantes de citas/agenda para el vertical `comerciales`) — nuevos widgets `contactos-nuevos` y `visitas-comercial`, regla anti-repetición (`MAX_AGENDA_GROUP_DEFAULTS=2`) en `defaultDashboardWidgets`.
- [x] 6.3 Widget de Agenda del dashboard agrandado a tamaño `xl` con card lateral (mismo layout que `/citas`), antes era una versión mini.
- [x] 6.4 Fix Google Maps `InvalidKeyMapError`: `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` en `front/.env.local` tenía un `:` sobrante al inicio (40 chars en vez de 39) — corregido.
- [x] 6.5 Rebrand Telegram → Minion: ícono SVG propio (`MinionIcon`), texto "Minion" en vez de "Telegram" en toda la UI.
- [x] 6.6 Bug real de tiempo real: el polling de 5s pisaba el mensaje optimista recién enviado con datos viejos del servidor — fix con merge por id en `use-telegram-inbox.ts` en vez de reemplazo total del estado.
- [x] 6.7 Widget de Minion pasa de estar montado solo dentro de `(crm)/layout.tsx` (por proyecto) a `app/layout.tsx` (root) — ahora aparece en `/dashboard` y persiste al entrar/salir de cualquier proyecto.
- [x] 6.8 Terminología "Cuenta"→"Cliente" en columna de Citas/Reuniones para el vertical `comerciales`.
- [x] 6.9 20 clientes + 10 contactos (mix lead/prospecto) mock creados en el tenant "Comercial Demo IA" vía SQL directo, todos los campos rellenos, geo real de Madrid.
- [x] 6.10 Empleado de prueba "Laura Fichaje Test" creado en el tenant "AiAs" para poder probar fichaje — SIN login todavía (crear vía Configuración → invitar usuario, no se hizo por SQL directo al no tener credenciales de admin para pasar por el flujo real).

## Phase 7: Refinamiento Clientes/Contactos tras feedback (2026-07-05, 2ª pasada)
- [x] 7.1 Confirmar/afinar filtros código, nombre, email, sector, contactado, fecha en Contactos (los 6, ya existen) y en Cartera de Clientes (solo nombre/email/fecha tienen campo real en `Customer` — código/sector/contactado no existen en ese modelo, dejar documentado de nuevo). Ambas páginas ya estaban correctas; sin cambios.
- [x] 7.2 Columnas Cartera de Clientes: Id Cliente, Nombre, Teléfono, Email, botón de información (Visitas total + Gasto total/pendiente de cobro), Acciones — quitar columna Segmento. Hecho en `front/app/(crm)/clientes/page.tsx`; `gastoPendiente` agregado server-side en `back/src/routes/customers.ts` (invoice.groupBy por nombre, estado != 'Pagada').
- [x] 7.3 Columna "Facturas" separada de Acciones (propia, no botón mezclado). Hecho.
- [x] 7.4 Ícono de Facturas = mismo que agents-agency (documento genérico, `FileText` en vez de `ReceiptText`). Hecho en `front/lib/config/modules.ts` y `shared/generate/tenant-types.ts`.
- [x] 7.5 En el modal de información del cliente, junto a la dirección: pin 📍 clicable que abra Google Maps. Hecho, reutilizando `lib/comercial/maps-link.ts` (coords) con fallback a `lib/citas/google-maps-url.ts` (texto de dirección).
- [x] 7.6 Confirmar colores de acciones (`tone` de `IconButton`) aplicados de forma consistente en Clientes y Contactos, iguales a agents-agency. Ya estaban correctos en ambas páginas; sin cambios.
- [x] 7.7 Badge en el ítem "Contactos" del sidebar de creador_CRM con el conteo de leads con `contactado != 'si'` (no existía). Backend `GET /contactos/pending-count` YA existía (scoped por `req.businessId`); solo faltaba el consumo en `front/components/layout/sidebar.tsx` — hecho.
- [x] 7.8 Investigado y corregido el badge equivalente en `agents-agency` (paralelo, otro agente): el query server-side ya filtraba bien (`contactado: {not:'si'}`, confirmado contra datos reales: 20 "no" + 4 "si"). Causa real: el badge de `Sidebar.tsx` solo refrescaba en cambio de ruta (`[pathname]`) — si el usuario marcaba contactos como "si" sin salir de `/contactos`, quedaba con el número previo. Fix: evento global `CONTACTS_UPDATED_EVENT`/`notifyContactsUpdated()` disparado tras alta/edición/borrado/cambio de estado, escuchado además del refetch por pathname.

## Phase 8: Refinamiento Clientes 3ª pasada (2026-07-05)
- [x] 8.1 Columna "Empresa"/razón social + columna "Contacto". Campo nuevo `Customer.razonSocial` (nullable), migración `20260705020000_customer_razon_social` desplegada a Supabase real; poblado en los 20 clientes mock de "Comercial Demo IA". `nombre`/`apellido` se quedan igual (persona), solo cambió el label de columna a "Contacto".
- [x] 8.2 Reordenado: `[..., 'Facturas', 'Acciones']` (Facturas antes que Acciones).
- [x] 8.3 Los 3 íconos de Acciones estaban repartidos entre una columna `''` suelta ("ver") y "Acciones" (editar/borrar) — consolidados en una sola columna Acciones, en línea.
- [x] 8.4 `eurSuffix()` nuevo en `front/lib/utils/format.ts`, aplicado a Gasto total y Gasto pendiente de cobro.
- [x] 8.5 `buildPinUrl()` nuevo en `maps-link.ts` (URL de punto/marcador simple) para el pin del modal de Clientes; `buildRouteUrl` intacto en su uso original (mapa comercial, botón "Ir").
- [x] 8.6 Causa real confirmada: `config_negocio.datos.modules.facturas = false` para el tenant, y `comerciales` nunca tuvo `facturas` en `defaultModules`. Activado para el tenant vía SQL y agregado a `defaultModules` del vertical (la tabla de Clientes siempre muestra la columna Facturas, dejarlo fuera reproducía el bug en cualquier negocio nuevo de este vertical).
- [x] 8.7 5 facturas mock creadas en "Comercial Demo IA" (SQL directo, ligadas por nombre a clientes ya existentes, estados Pagada/Pendiente mixtos).

## Phase 9: Contactos info + dirección estructurada + mapa comercial + Presupuestos/Facturas (2026-07-06)
Regla de esta fase: una tarea por vez, sin commitear ni pasar a la siguiente sin OK explícito del dueño del proyecto.
- [x] 9.1 Pin de Google Maps también en el modal de información de Contactos (Contacto no tiene lat/long, usa `buildGoogleMapsSearchUrl` por texto siempre). El pin de Clientes se ajustó para priorizar TAMBIÉN el texto de dirección (antes priorizaba coords aproximadas) — mismo comportamiento confiable en ambas tablas.
- [x] 9.2 Dirección estructurada (dirección, número, piso, código postal) en Clientes Y Contactos. Migración `20260706000000_structured_address` aplicada. `buildAddressQuery` aditivo (número mejora la query, ausencia = comportamiento idéntico). 2ª pasada: `localidad` añadida a Contactos (migración `20260706010000_contacto_localidad`).
- [x] 9.3 Mapa comercial: quitados los `<select>` de estado/categoría; zona desglosada en 3 inputs (localidad/provincia/código postal) con filtros AND independientes en backend (`buildListFilters`, `zona` OR eliminado); leyenda del selector de color ahora clicable con toggle single-select (click filtra, click en el activo quita); botón de modo de color pasa a emerald cuando hay filtro activo en esa dimensión.
- [~] 9.4 Presupuestos/TPV/Facturación — DESCOMPUESTA en la Fase 10 (ver abajo). Decisiones del dueño (2026-07-06): "Presupuestos" = módulo NUEVO desde cero (flujo AA, modelo propio SIN imagen); "Pedidos" actual se deja intacto (módulo distinto); el módulo de cobro/caja (`ventas`, hoy etiquetado "Pedidos" en comercial) se renombra a "TPV"; facturas DETALLADAS como AA (líneas + IVA). Todo pega a BD (migraciones). Editar aunque NO esté rechazada. Quitar columna docs de facturas + asegurar columna fecha. Ordenar por nº/cliente/fecha/estado.

## Phase 10: Presupuestos (nuevo) + TPV rename + Facturación detallada (2026-07-06) — descomposición de 9.4
Regla: una tarea por vez, Opus para los builds, verificar (tsc + suites + una sola revisión) y commit LOCAL sin push, esperar OK del dueño antes de la siguiente.
- [x] 10.1 Renombrado el módulo de cobro/caja a "TPV": terminología `ventas: 'Pedidos'` → "TPV" en el vertical `comerciales` (default en `verticals.ts`) + fix en la config GUARDADA del tenant "Comercial Demo IA" (deserialize respeta overrides guardados). Además activado `modules.ventas=true` en ese tenant (estaba OFF) para que el nav "TPV" aparezca. Sin tocar ruta `/ventas` ni lógica de cobro. Test de naming actualizado.
- [x] 10.2 REPURPOSAR `pedidos` como "Presupuestos" (decisión dueño: el modelo `Pedido` ya ERA el espejo exacto del Budget de AA — mismos estados, snapshots, líneas, IVA, auto-factura; NO se duplica modelo). Renombrar el label del módulo `pedidos` → "Presupuestos" (defaultLabel + description + subtítulo + botón; el doc imprimible ya dice "Presupuesto"). Mantener id/ruta `pedidos` internos para no romper `config.modules` guardados ni datos. Completar paridad AA: EDITAR un presupuesto en CUALQUIER estado (no solo rechazada) persistiendo a BD (PATCH /pedidos/:id, hoy solo hay create); ordenar por número/cliente/fecha/estado (server-side, patrón de 9.11); campo Razón Social en el form; sin imagen en el modelo/preview. `Pedidos` como módulo separado NO se crea (el usuario lo descartó al elegir repurposar). HECHO: label `pedidos`→"Presupuestos" (id/ruta intactos), copy visible actualizado, `PATCH /pedidos/:id` (edita en cualquier estado salvo aceptada→409 para no desincronizar la factura), `buildPedidosOrderBy` (número/estado/createdAt server-side; cliente client-side por vivir en snapshot JSON), campo Razón Social en form+snapshot, sin imagen. Sin migración (snapshots JSON). back 526 / front 572 verde.
- [x] 10.3 Facturación DETALLADA como AA: rework del modelo Invoice a líneas + IVA (migración; preservar datos/facturas existentes), preview detallado con tabla de líneas y desglose de impuestos; `PUT /invoices/:id/status` con enum cerrado + editar estado desde la fila; quitar columna "Docs" y asegurar columna "Fecha"; ordenar por número/cliente/fecha/estado; KPI "importe cobrado" + campo `pagadaEn`. HECHO: `InvoiceLine` (linea_factura, forma cantidad+precioUnit+importe; línea impl+mant del pedido se PARTE en "(pago único)"/"(mensual)" para exactitud al céntimo) + subtotal/tasaIva/pagadaEn en Invoice; migración `20260706030000_invoice_lines_iva` APLICADA a Supabase y verificada (5/5 facturas: total EXACTO, 1 línea c/u, suma cuadra, tasaIva=0 legacy) + `prisma migrate resolve --applied` (status limpio, 25 migraciones); estados reales en BD: Pendiente/Pagada, ambos en el set cerrado Pendiente|Pagada|Anulada; `ensureInvoiceForPedido` snapshotea líneas+desglose (total EXACTO totalImpl+totalMant); operador (bot) también autocontenido; `PUT /invoices/:id/status` (DI handler, 422 fuera del set, pagadaEn set/clear); `estado` Y `total` FUERA de la whitelist PATCH (total es snapshot/derivado — hardening tras review sdd-verify PASS-WITH-NOTES, test PATCH actualizado); `buildInvoicesOrderBy` (numero/cliente/fecha/estado, todos escalares → server-side); front: preview con tabla de líneas + Base imponible/IVA/Total (IVA por diferencia para cuadrar), sin columna Docs, Fecha presente, orden por cabecera, badge de fila cicla estados, KPI "Importe cobrado" (metrics.importePagado ya existía server-side, ahora visible). back 545 / front 578 verde.
- [x] 10.4 Genera una lista de presupuestos sobre productos de interiorismo y paisajismo. HECHO: script `back/src/scripts/seed-presupuestos-demo-live.ts` (idempotente por negocio+numero, totales vía `computePedidoTotals`, aceptadas auto-generan factura vía `ensureInvoiceForPedido` en la misma transacción). 12 presupuestos sembrados en el tenant "Comercial Demo IA" (PRES-2026-001..012, 7 generada/3 aceptada/1 rechazada/1 caducada), enlazados a 12 clientes reales distintos; catálogo interiorismo (proyecto salón, reforma cocina, parquet, microcemento, mobiliario a medida...) + paisajismo (jardín mediterráneo, pérgola bioclimática, riego por goteo, estanque, césped, mantenimiento mensual...). Verificado en Supabase: 12/12 subtotales cuadran, 3/3 aceptadas con 1 factura y total=totalImpl+totalMant al céntimo, subtotal factura = Σ líneas. tsc limpio, sin tocar los 21 clientes/10 contactos.
- [x] 10.5 Swagger AA+CRM actualizado + prisma redeployado. CRM `swagger.ts`: Customer (razonSocial/dirección estructurada/geo/gastoPendiente + filtros/sort), y documentados DESDE CERO Contactos (incl. geocode/rerun, pending-count, convert), Pedidos (+PUT status), Invoices (líneas+IVA, PUT status, PATCH sin estado/total); Employees (materialización admin comerciales), Bookings (notas `Acción|Canal`), Projects PATCH. AA `swagger.ts`: reconciliado — documentado `/api/invoices` que existía sin cobertura. `prisma generate` CRM+AA OK; migrate status CRM 25 up-to-date. GOTCHA resuelto: AA reportaba `20260704120000_add_invoice` sin aplicar, pero `aa.factura` YA EXISTÍA en Supabase (columnas idénticas) → drift de historial por db push, NO bug 500; `migrate resolve --applied` → AA 4 migraciones up-to-date. tsc CRM+AA limpio; specs cargan (CRM 25 paths/13 schemas, AA 22 paths/11 schemas).
- [x] 9.11 CORRECCIÓN hecha: quitados los filtros de columna de Clientes (nombre/email/fecha) y Contactos (código/tipo/nombre/email/sector/fecha), reemplazados por ordenación al pulsar cabecera. `Table`/`TableHeadCell` con toggle asc/desc + indicador + aria-sort. Clientes ordena por Id Cliente/Empresa/Contacto/Email; Contactos por código/tipo/nombre/email/sector/fecha. Server-side en modo API (`buildCustomersOrderBy`/`buildContactosOrderBy` con whitelist), client-side en mock. Solo queda el buscador de texto. `tipo`/`contactado` se conservan en el backend (los usa el badge de pendientes).
- [x] 9.12 Mapa comercial muestra Clientes Y Contactos. Migración `20260706020000_contacto_geo` (lat/long/geoEstado en Contacto, aplicada a Supabase). Geocoding: `POST /contactos/geocode/rerun` + geocode en create/update (reusa `resolveGeocoder`); los 10 contactos mock geolocalizados (10/10 OK). Render: clientes = círculo (color estado/gasto), contactos = rombo violeta neutro (`map-point.ts`, entrada de leyenda). `buildBoundsKey` refleja ambas capas sin resetear zoom por selección. Click en contacto → card ligera (no la ficha completa de cliente).

### Fixes intercalados durante la Fase 10 (bugs reportados en vivo, antes de 10.5)
- [x] F10.a Agenda "Nueva cita" (vertical comerciales): campo **Acción** como combobox editable (`<input list>` + datalist de 8 acciones comerciales + texto libre), guardado en `notes` con formato canónico `Acción: <x> | Canal: <y>` (`buildCitaNotes`); parser de `shared.tsx` lee ambos campos anclado al inicio de segmento (retrocompatible con `Canal: X` legacy; texto libre con `:`/`|` no corrompe el parseo — saneo de `|` + regresión testeada). Canal añade **"Llamada"**. Admin como **Employee real** en comerciales: `ensureAdminEmployee(businessId)` (clave idempotente (businessId, email) porque `Employee.userId` es @unique global; catch P2002 para concurrencia), invocado best-effort en GET /employees (try/catch: no tumba la lista). Backfill del tenant demo (1 empleado admin). back 551 / front 597 verde.
- [x] F10.b Seed de citas en vivo para "Comercial Demo IA": `seed-citas-comercial-demo-live.ts` idempotente → 22 citas (5 junio / 17 julio 2026), mitad clientes reales / mitad leads (nombre del lead en notes), 5 servicios comerciales, formato de notas canónico, estados variados. Verificado en Supabase.
- [x] F10.c Persistencia de la card de proyecto en OperaOS: "Editar"→onboarding→"Guardar cambios" ahora **persiste a BD** (`updateProject(id,cfg)` awaited con id explícito → PATCH /projects/:id; arregla el bug del `activeId` stale del `setConfig` best-effort). "Abrir"→/panel queda **efímero** (se quitó el PATCH fire-and-forget de `setConfig`; sólo el guardado explícito del onboarding publica a BD). Navega sólo si el guardado fue OK. front verde + tests nuevos.

## Phase 11: Horario de negocio + pin en cita + horas disponibles (2026-07-06)
Decisión de arquitectura (dueño): el horario del negocio REUSA el modelo existente `OpeningHour` (`horario_apertura`; varias filas por (locationId, diaSemana) = horario partido). Sin modelo nuevo ni migración. Referencia a espejar: horario de empleado (`GET/PUT /employees/:id/horario` + `horario-empleado-modal.tsx`).
- [x] 11.1 FIX pin en modal de detalle de cita: botón `MapPin` a la IZQUIERDA en la misma fila que "Guardar anotación" (`justify-between`), abre `direccion` en Google Maps (search URL) en pestaña nueva; oculto sin dirección. Tests en `cita-detalle-modal.test.tsx`.
- [x] 11.2 BUG "horas disponibles" vacías en calendario del demo: causa raíz = 0 filas `OpeningHour` en la sucursal de "Comercial Demo IA" (`iterateDaySlots` genera chips desde ahí). Seed en vivo idempotente `back/src/scripts/seed-horario-comercial-demo-live.ts` (L-V 09-14 + 16-19 partido, S 10-14, D cerrado) + verificación in situ con `daySlotsWithAvailability` (miércoles/sábado con chips, domingo vacío).
- [x] 11.3 FEATURE horario del negocio en onboarding (paso "Datos", bajo la dirección): toggle continuo/partido, `<input type="time">` (teclado + ratón), checkboxes L M X J V S D, GRUPOS múltiples de días (día sin grupo = cerrado; exclusividad last-wins: marcar un día en un grupo lo quita de los demás). Persistencia doble: `TenantConfig.horario` (BusinessSetting, retrocompatible si falta) + aplanado a `OpeningHour` vía backend nuevo `GET/PUT /config/horario` (espejo exacto del horario de empleado: Tramo {diaSemana 0-6, inicio, fin}, deleteMany+createMany atómico, tenant-scoped, resuelve la Location [0] del negocio). `finish()` del onboarding (alta y edición, modo API) hace el PUT tras guardar — best-effort con aviso visible; en modo generador/mock solo queda en la config.
- [x] 11.4 Tests: e2e back `config-horario.e2e.test.ts` (reemplazo atómico, validación incl. inicio<fin + dedupe, scoping tenant, resolución de sucursal); front `schedule.test.ts` (aplanado, cerrado, partido/continuo, exclusividad, retrocompat TenantConfig) + `horario-negocio-form.test.tsx` (UI) + pin en `cita-detalle-modal.test.tsx`.
- [x] 11.5 Refinamiento (feedback dueño): `mode` intensiva/partida pasa de GLOBAL a **POR GRUPO** (`ScheduleGroup.mode`; `setGroupMode` solo toca ese grupo) + botón **Aceptar** por grupo (`aceptado?`, colapsa a resumen con "Editar"; solo estado de UI, no bloquea el aplanado). Retrocompat: `normalizeSchedule` migra el modelo antiguo (mode global → por grupo, inferido del nº de tramos) en `deserialize`. Reutilizado el componente `HorarioNegocioForm` en **Configuración → pestaña Negocio** (admin, bajo Dirección): precarga las horas reales con `scheduleFromTramos` (inverso de `scheduleToTramos`, round-trip testeado) vía GET `/config/horario`, y guarda con PUT explícito (el panel es efímero para el resto). Componente presentacional/reutilizable. front 630 / back 551 verde.
- [x] 11.6 Imagen de marca 2 (`logoImage2`, opcional/retrocompat): campo propio en el paso 3 de `BrandingForm` (uploader/preview/quitar independientes; si vacío muestra la del paso 2 atenuada, nunca iniciales) en onboarding y Configuración → Marca. Cabecera de `pedido-preview`/`factura-preview` = `logoImage2 || logoImage` (tamaño acotado, nada si ambas vacías); leída con nuevo hook `useTenantBranding()`. front 640 verde.
- [x] 11.7 Bordes de celda del calendario (`.calendar-day`/`.agenda-week-col`) con `var(--line)` theme-aware (claro y oscuro; `.empty` sin borde), aplica a widget y `/citas`. Pin de ubicación del modal Detalle de Cita ahora SÍ aparece: la dirección se resuelve `back/src/lib/citaDireccion.ts` (dirección estructurada del CLIENTE visitado > fallback `Location.direccion`), añadida a `GET /bookings` y propagada a `CitaConNotas.direccion`; el modal muestra además un registro **"Dirección"** (oculto si vacío). front 642 / back 559 verde.

### Fixes intercalados durante la Fase 9 (bugs reportados en vivo, antes de continuar con 9.2)
- [x] 9.5 "Cerca de mí" (mapa comercial) geolocalizaba impreciso: faltaba `enableHighAccuracy` en `getCurrentPosition` (usaba resolución por IP/red). Agregado `enableHighAccuracy:true, timeout:10s, maximumAge:0`.
- [x] 9.6 Editar Cliente no persistía visualmente a la primera: `onSubmit` mutaba vía `useCollection` (fire-and-forget) mientras la tabla se pinta desde `usePaginatedApi` — el PATCH sí llegaba a persistir, pero `paged.refresh()` corría en paralelo y a veces perdía la carrera. Mismo defecto en Eliminar (la fila no desaparecía). Fix: `await` directo del PATCH/DELETE antes de `paged.refresh()`.
- [x] 9.7 Editar Contacto no persistía a la primera: causa distinta a Clientes — los 7 inputs de filtro disparan un GET por tecleo sin descartar respuestas fuera de orden; un GET viejo en vuelo podía resolver después del guardado y pisar la fila editada. Fix: guardia de secuencia (`fetchSeq`) en `fetchApi`, mismo patrón que ya protege a `usePaginatedApi`.
- [x] 9.8 19/20 clientes mock de "Comercial Demo IA" re-geocodificados con Nominatim real (mismo servicio que usa el botón "Re-geolocalizar"), reemplazando las coordenadas aproximadas por barrio de la Fase 6. El cliente restante ("Jorge Toledano") tenía `codigo_postal` desincronizado de su propia dirección (28004 guardado vs 28022 real) — corregido; con eso resuelto, Nominatim solo devuelve el tramo de calle (sin house-number para esa avenida en OSM) — límite real de datos del proveedor gratuito, no bug de código.
- [x] 9.9 Mapa comercial: el zoom/pan se reseteaba en cada interacción (seleccionar cliente, cambiar modo de color) porque `fitBounds()` corría en cada repintado de marcadores. Fix: solo reencuadra cuando cambia el conjunto real de clientes ubicados, no en cada selección.
- [x] 9.10 Sidebar: botón de contraer/ampliar portado de `agents-agency` (misma clave de localStorage `sidebar-collapsed`, mismo ancho `w-[68px]`/`w-60`, oculta labels/títulos de grupo/nombre de negocio al colapsar, footer compacto).
